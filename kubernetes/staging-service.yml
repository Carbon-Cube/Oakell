apiVersion: v1
kind: Service
metadata:
  name: oakell-service
  namespace: oakell-staging
spec:
  selector:
    app: oakell
  ports:
    - protocol: SCTP
      port: 443
      targetPort: 8080
  type: LoadBalancer