apiVersion: v1
kind: Service
metadata:
  name: oakell-service
  namespace: oakell-staging
spec:
  selector:
    app: oakell
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer